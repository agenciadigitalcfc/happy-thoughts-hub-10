import { Sparkles, HeartPulse, Ruler, ShieldCheck } from "lucide-react"; import {useI18n} from "@/lib/i18n"; import {Spotlight} from "./Spotlight";
export function StatsCounters(){const {t}=useI18n();const items=[["stats.item1.label",Sparkles],["stats.item2.label",Ruler],["stats.item3.label",HeartPulse],["stats.item4.label",ShieldCheck]];return <Spotlight tone="light"><section className="px-6 py-24" style={{background:"var(--gradient-champagne)"}}><div className="mx-auto max-w-6xl"><div className="text-center"><span className="eyebrow">{t("stats.eyebrow")}</span><h2 className="title-display mt-4 text-3xl md:text-5xl">{t("stats.title")}</h2></div><div className="mt-14 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{items.map(([key,I])=><article key={key as string} className="vellum rounded-[2rem] p-8 text-center"><I className="mx-auto size-7 text-gold"/><p className="title-display mt-5 text-2xl">{t(key as string)}</p></article>)}</div></div></section></Spotlight>}
