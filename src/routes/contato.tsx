import { createFileRoute } from "@tanstack/react-router";
import { Phone, MessageCircle } from "lucide-react";
import { PageShell } from "@/components/site/PageShell";
import { CLINIC, FIRST_VISIT, whatsappLink } from "@/lib/site";
import { useI18n } from "@/lib/i18n";
export const Route=createFileRoute("/contato")({head:()=>({meta:[{title:"Contato | Dr. Samuel Montalvão | Instituto Montalvão"},{name:"description",content:"Agende sua avaliação presencial ou online com o Dr. Samuel Montalvão."}]}),component:Contato});
function Contato(){const {t}=useI18n();return <PageShell eyebrow={t("nav.contact")} title={<>{t("contact.title")}</>} intro={t("contact.desc")}><section className="px-6 py-20"><div className="mx-auto max-w-5xl"><div className="grid gap-5 sm:grid-cols-2"><div className="vellum rounded-3xl p-7"><Phone className="size-5 text-gold"/><p className="eyebrow mt-5">WhatsApp</p><p className="mt-2 text-sm text-text-soft">{CLINIC.phone}</p></div><div className="vellum rounded-3xl p-7"><MessageCircle className="size-5 text-gold"/><p className="eyebrow mt-5">Avaliação</p><p className="mt-2 text-sm text-text-soft">Presencial ou online · {FIRST_VISIT.price}</p></div></div><div className="mt-12 vellum rounded-[2rem] p-8"><span className="eyebrow">{t("contact.visit.title")}</span><h2 className="title-display mt-3 text-2xl md:text-3xl">{t("contact.visit.subtitle")}</h2><ul className="mt-6 grid gap-3 text-sm leading-relaxed text-text-soft">{[1,2,3,4,5].map(i=><li key={i} className="flex gap-3"><span className="mt-2 size-1.5 shrink-0 rounded-full bg-gold"/>{t(`contact.visit.step${i}`)}</li>)}</ul><p className="mt-6 text-sm text-text-soft">{FIRST_VISIT.note}</p></div><div className="mt-12 text-center"><a href={whatsappLink()} target="_blank" rel="noopener noreferrer" className="btn-gold-lux" data-magnetic>Agendar avaliação pelo WhatsApp</a></div></div></section></PageShell>}
