import { createFileRoute } from "@tanstack/react-router";
import { Instagram } from "lucide-react";
import { PageShell } from "@/components/site/PageShell";
import { TEAM, whatsappLink } from "@/lib/site";
import { useI18n } from "@/lib/i18n";
export const Route=createFileRoute("/equipe")({head:()=>({meta:[{title:"Equipe | Instituto Montalvão | Dr. Samuel Montalvão"},{name:"description",content:"Conheça os profissionais apresentados pelo Instituto Montalvão."}]}),component:Equipe});
function Equipe(){const {t}=useI18n();return <PageShell eyebrow={t("team.eyebrow")} title={<span>{t("team.title")}</span>} intro={t("team.intro")}><section className="px-6 py-20"><div className="mx-auto grid max-w-6xl gap-5 sm:grid-cols-2 lg:grid-cols-3">{TEAM.map(p=><article key={p.id} data-reveal className="vellum vellum-hover rounded-3xl p-7"><span className="eyebrow">{t(`team.member.${p.id}.role`)}</span><h2 className="title-display mt-3 text-xl">{p.name}</h2><p className="section-lede-sm mt-3">{t(`team.member.${p.id}.desc`)}</p><div className="mt-6 flex gap-4"><a href={whatsappLink(`Olá! Gostaria de agendar com ${p.name}.`)} target="_blank" rel="noopener noreferrer" className="btn-slide-gold text-foreground">Agendar</a>{p.instagram&&<a href={p.instagram} target="_blank" rel="noopener noreferrer" className="flex items-center gap-2 text-gold"><Instagram size={14}/>Instagram</a>}</div></article>)}</div></section></PageShell>}
